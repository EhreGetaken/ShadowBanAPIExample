package net.shadowapi.types;

public enum ShadowType {

    ERROR_ALREADY_CONNECTED,
    ERROR_AUTH_KEY_NOT_VALID,
    ERROR_NOT_CONNECTED,
    CONNECTED,
    REQUEST_BANNED,
    REQUEST_TYPE,
    TIMEOUT;

}
